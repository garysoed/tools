<a name="{{anchor}}" class="tsd-anchor"></a>
<md-subheader class="gs-sticky {{cssClasses}}">
  <h3>
    {{#if name}}
      <span class="gs-member__flags gs-code">
        {{#each flags}}{{this}}{{/each}}
      </span>
      <span class="gs-member__signature">
        {{> gs.signature}}
      </span>
      <span class="gs-member__source">
        {{#if sources}}
          {{#each sources}}
            {{> code.link}}
          {{/each}}
        {{/if}}
      </span>
    {{/if}}
  </h3>
</md-subheader>

<md-divider class="{{cssClasses}}"></md-divider>
<section class="gs-member__definition {{cssClasses}}">
  {{#if signatures}}
  {{> member.signatures}}
  {{else}}{{#if hasGetterOrSetter}}
  {{> member.getterSetter}}
  {{else}}
  {{> member.declaration}}
  {{/if}}{{/if}}
</section>
<md-divider class="{{cssClasses}}"></md-divider>
{{#if groups}}
  <section class="gs-member__children {{cssClasses}}">
    {{#each groups}}
      {{#each children}}
        {{#unless hasOwnDocument}}
          {{> member}}
        {{/unless}}
      {{/each}}
    {{/each}}
  </section>
  <md-divider class="{{cssClasses}}"></md-divider>
{{/if}}
